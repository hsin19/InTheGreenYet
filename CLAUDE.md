@AGENTS.md

## Skills

Reusable task procedures live as `SKILL.md` files. When a task matches the
description below, **read the full file before acting** — don't work from this
summary alone.

- **add-provider** — connect a new account provider's live-balance API
  (crypto exchange / stock broker / bank): a Worker signing proxy plus a
  declarative frontend entry. → `.agents/skills/add-provider/SKILL.md`
- **workers-best-practices** — review or author Cloudflare Worker code against
  production best practices (streaming, floating promises, global state,
  secrets, bindings, observability). → `.agents/skills/workers-best-practices/SKILL.md`
- **web-design-guidelines** — review UI code for Web Interface Guidelines
  compliance; use for UI review, accessibility, design audits, and UX checks.
  → `.agents/skills/web-design-guidelines/SKILL.md`
- **wrangler** — correct syntax and best practices before running `wrangler`
  commands (dev, deploy, types, secrets, bindings). → `.agents/skills/wrangler/SKILL.md`
