# Single-package repo (no `packages:` list). This file only carries pnpm
# settings — notably which dependencies are allowed to run install scripts.
allowBuilds:
  '@swc/core': false
  dprint: true
  esbuild: false
  sharp: false
  simple-git-hooks: true
  workerd: true
